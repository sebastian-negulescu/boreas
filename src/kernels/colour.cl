#include "object.h"
#include "vec.h"
#include "pane.h"

__kernel void colour_path(
    const vec3 origin, 
    const pane p, 
    const uint num_objects, 
    __global object *objects
) {
    return;
}

